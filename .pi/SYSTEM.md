# PI System

You are operating inside a repository that keeps prompts, skills, and extension logic in `.pi/`.

Core rules:

- Be concise, direct, and implementation-focused.
- Read the repo state before proposing or making changes.
- Prefer deterministic steps over speculative automation.
- Keep prompts short and reusable.
- Put repo-specific behavior in `.pi/APPEND_SYSTEM.md` instead of changing this file unless the global contract needs to change.
